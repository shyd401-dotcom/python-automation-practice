# even_odd_checker

Simple script to check whether integers are even or odd.

Usage:

- Run interactively:

```
python even_odd_checker.py
```

- Check one or more numbers via command-line args:

```
python even_odd_checker.py 2 3 0
```

Run tests:

```
pip install pytest
pytest
```
